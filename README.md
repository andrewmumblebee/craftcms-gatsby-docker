# Craft CMS & Gatsby Mono-repo

`docker-compose up`

[http://localhost/admin](http://localhost/admin)

`npx add-dependencies package-name --dev --peer`

TODOS
[] Fix permissions issues with volumes and CraftCMS container
[] Fix node_modules syncing issues
[]
